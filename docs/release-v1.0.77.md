# 豌豆AI工具 v1.0.77

- 新增智能分层 PSD 节点，可按画面内容拆分主体、文字、装饰元素与光效，并导出分层 PSD。
- 优化扩图画布的预览、比例调整与更换图片操作；扩图与绘图可按 GPT 模型能力选择透明背景。
- 修复扩图后把原图硬贴回结果中央造成矩形接缝的问题。接口若重绘原图或返回不匹配的比例，结果会保留完整生成画面并提示检查，不再强制拼接或拉伸。
- 改进图片接口请求取消、返回结果识别与任务状态反馈。

本版通过本地非浏览器回归测试；Windows 打包与端到端测试由发布流水线执行。AI 模型生成效果取决于所用接口，生成图中的主体及边缘仍应在下载前检查。
