# PSD writer

`ag-psd.bundle.js` is the browser bundle from `ag-psd` version 31.0.2, published at https://www.npmjs.com/package/ag-psd. It is loaded only when the layered PSD workflow exports a file. The package is MIT licensed; see `ag-psd.LICENSE`.
